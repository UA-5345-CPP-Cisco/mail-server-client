name: CI Build & Test

on:
  pull_request:
    branches:
      - main
      - 'development/**'
    paths:
      - 'apps/**/*.cpp'
      - 'apps/**/*.h'
      - 'apps/**/*.hpp'
      - 'apps/**/*.qml'
      - 'libs/**/*.cpp'
      - 'libs/**/*.h'
      - 'libs/**/*.hpp'
      - 'tests/**/*.cpp'
      - 'tests/**/*.h'
      - 'tests/**/*.hpp'
      - '**/CMakeLists.txt'
      - 'cmake/**/*.cmake'
      - 'CMakePresets.json'
      - '.clang-format'
      - 'scripts/*.sh'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest 

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg lsb-release software-properties-common libsqlite3-dev libssl-dev qt6-declarative-dev-tools
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 22
          sudo apt-get install -y clang-format-22
          clang-format-22 --version
          if command -v qmlformat >/dev/null 2>&1; then
            qmlformat --version
          elif [[ -x /usr/lib/qt6/bin/qmlformat ]]; then
            echo "/usr/lib/qt6/bin" >> "$GITHUB_PATH"
            /usr/lib/qt6/bin/qmlformat --version
          else
            echo "qmlformat not found"
            exit 1
          fi

      - name: Check formatting
        run: CLANG_FORMAT=clang-format-22 scripts/check-format.sh

      - name: Configure CMake
        run: >
          cmake -B build
          -DMAIL_BUILD_TESTS=ON
          -DMAIL_BUILD_QT_CLIENT=OFF

      - name: Build
        run: cmake --build build --parallel $(nproc)

      - name: Run tests
        run: ctest --test-dir build -LE cryptopp --output-on-failure
