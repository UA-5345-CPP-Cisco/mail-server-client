services:
  qt:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      DISPLAY: ${DISPLAY}
    working_dir: /workspace